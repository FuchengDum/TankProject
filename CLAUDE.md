# CLAUDE.md

本文件为 Claude Code (claude.ai/code) 在本仓库工作时提供指导。

## 项目概述

这是一个用 pygame 实现的经典坦克大战游戏（类似于 Battle City）。游戏包含玩家坦克、敌方坦克、各种地形、道具和多关卡。

## 代码架构

### 核心组件

1. **主游戏循环**：`main.py` - 包含主要的游戏循环和状态管理
2. **游戏逻辑**：`game_loader.py` - 负责游戏初始化、状态更新和渲染
3. **地图系统**：`maps.py` - 包含 35 个预定义关卡和地图元素类（砖块、铁块、河流、树、冰、家）
4. **玩家坦克**：`myTank.py` - 实现玩家坦克的行为、移动、射击和升级
5. **敌方坦克**：`enemyTank.py` - 实现敌方 AI、移动模式和射击
6. **子弹系统**：`bulletClass.py` - 处理子弹移动、碰撞检测
7. **道具**：`food.py` - 实现各种道具（共 7 种类型）
8. **特效**：`special_effects.py` - 处理爆炸、坦克出现和保护罩等动画效果
9. **地形元素**：`wall.py` - 定义不同类型的地形元素（砖块、铁块、河流等）

### 关键设计模式

1. **基于精灵的架构**：所有游戏对象都继承自 `pygame.sprite.Sprite`
2. **地图数据结构**：关卡以二维数组定义，每个数字代表一种地形类型
3. **组件化设计**：坦克组合子弹而不是继承子弹
4. **状态管理**：通过各种布尔标志和计数器管理游戏状态

## 常见开发任务

### 运行游戏
```bash
python3 main.py
```

### 添加新关卡
1. 在 `maps.py` 中添加新的地图数组（遵循现有模式）
2. 更新 `checkpoint` 方法以包含新关卡
3. 确保关卡编号在游戏状态逻辑中正确处理

### 添加新道具
1. 将道具图片添加到 `image/food/`
2. 在 `food.py` 中添加新的道具类型
3. 在游戏逻辑中实现道具效果

### 修改坦克行为
1. 玩家坦克行为在 `myTank.py` 中
2. 敌方 AI 在 `enemyTank.py` 中
3. 两个文件都包含移动、射击和碰撞逻辑

### 添加新特效
1. 将精灵图添加到 `image/special_effects/`
2. 在 `special_effects.py` 中加载和管理新特效
3. 在主游戏循环中调用特效渲染方法

## 文件依赖关系

- 大多数文件依赖 `path_utils.py` 进行资源加载
- 游戏对象依赖 `wall.py` 进行地形定义
- 特效依赖 `special_effects.py`
- 所有 pygame 相关文件需安装 pygame

## 测试

本仓库未包含自动化测试。测试方式为手动运行游戏并通关。

## 调试技巧

1. 检查控制台的 pygame 错误信息
2. 使用 print 语句跟踪游戏状态变化
3. 通过 `path_utils.py` 验证图片路径是否正确
4. 确保所有精灵组都正确更新和绘制

## 常见问题

1. **图片缺失**：确保所有图片文件都在正确目录下
2. **导入错误**：检查所有依赖是否已安装
3. **碰撞检测**：精灵碰撞需准确的 rect 定位
4. **性能问题**：大量精灵可能影响帧率